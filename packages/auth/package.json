{
  "name": "@uttily/auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:fast": "vitest run",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@uttily/config": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
