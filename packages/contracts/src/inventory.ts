/**
 * Limites partagées du contrat d'inventaire.
 *
 * Ce module reste dépourvu de dépendance serveur afin d'être utilisable par
 * les Server Actions comme par les composants client.
 */
export const MAX_BULK_INVENTORY_ITEMS = 50;
